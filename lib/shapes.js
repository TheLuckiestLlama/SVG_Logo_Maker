// Call a function to dicate the display, then one to render it.


class Shapes {
    constructor(logoText, textColor, logoShape, logoColor){
    this.text = logoText;
    this.textColor = textColor;
    this.logoShape = logoShape
    this.logoColor = logoColor;
    }
}

module.exports = Shapes;