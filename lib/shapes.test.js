// Testing file to make sure this works
const jest = require('jest');
const Shapes = require('./shapes');


describe('generateLogo', () => {

    // TODO: Make test to be sure code works
    describe('renderLogo', () => {
        it("Should take the logo's color and shape, and render it accordingly.");
        let input = "MLG"
        const text = new Shapes
        expect(input = "MLG");
    })
})